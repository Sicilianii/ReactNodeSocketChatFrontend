export default function UserNetworkStatus__shared() {
    return (
        <section className={'user-net-status'}>
            <span className={'user-net-status__marker'}></span>
            <span className={'user-net-status__des'}>Online now</span>
        </section>
    );
}